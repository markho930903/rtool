export interface StoredWindowLayout {
  width: number;
  height: number;
  x: number;
  y: number;
}

export interface WindowLayoutBounds {
  monitorWidth: number;
  monitorHeight: number;
  minWidth: number;
  minHeight: number;
}
